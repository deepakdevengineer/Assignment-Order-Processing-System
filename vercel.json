{
  "version": 2,
  "buildCommand": "cd frontend && npm install --include=dev && npx ng build",
  "outputDirectory": "frontend/dist/frontend/browser",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
